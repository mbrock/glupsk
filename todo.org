#+title: Glupsk Refactoring Tracker
#+startup: overview
#+todo: TODO NEXT WAIT DONE

* Purpose

This file tracks architecture and refactoring work that is valuable but not
necessary to do in the middle of feature work. The bias is toward clearer
boundaries, fewer duplicated VM rules, and keeping backend-specific IO separate
from common Glulx and Glk semantics.

* DONE Split VM execution helpers out of core/execute.cpp
:PROPERTIES:
:Area: vm
:Value: high
:END:

~core/execute.cpp~ is currently the largest file and mixes several layers of
the interpreter. Keep opcode dispatch there, but extract the helper subsystems
that have their own semantics and tests.

** DONE Extract operand and store-destination helpers
:PROPERTIES:
:Suggested_File: core/vm_operands.hpp core/vm_operands.cpp
:END:

Move ~StoreRef~, ~Arguments~, operand load/store helpers, store operand
metadata, and load-width helpers into a reusable module.

This should also give tracing code a non-mutating way to inspect operands
without reimplementing part of the interpreter.

Implemented in ~core/vm_operands.hpp~ and ~core/vm_operands.cpp~. The module
now owns operand loading, store destinations, temporary call arguments, and
opcode-specific store/load metadata.

** DONE Extract stack frame and call helpers
:PROPERTIES:
:Suggested_File: core/vm_frames.hpp core/vm_frames.cpp
:END:

Move call stubs, function entry, local layout, frame access, return, and branch
helpers into a frame/call module.

The goal is to make the Glulx call ABI readable independently of the giant
opcode switch.

*** DONE Extract call stub and return/branch primitives

Implemented in ~core/vm_frames.hpp~ and ~core/vm_frames.cpp~. This covers call
stub push/pop, value-stack base calculation, function return, and branch
offset handling.

*** DONE Move function entry and local frame initialization

~enter_function~ now lives in ~core/vm_frames.cpp~ after the acceleration
machinery was split out behind ~core/vm_accel.hpp~.

** DONE Extract Glulx string output machinery
:PROPERTIES:
:Suggested_File: core/vm_strings.hpp core/vm_strings.cpp
:END:

Move string object decoding, compressed string/Huffman traversal, and related
output helpers out of the dispatcher file.

This is a distinct subsystem with tricky rules, so it should be easy to read
and test in isolation.

Implemented in ~core/vm_strings.hpp~ and ~core/vm_strings.cpp~.

** DONE Extract search opcode helpers
:PROPERTIES:
:Suggested_File: core/vm_search.hpp core/vm_search.cpp
:END:

Move linear, binary, and linked search helper logic into a focused module.

Implemented in ~core/vm_search.hpp~ and ~core/vm_search.cpp~.

** DONE Extract Inform acceleration support
:PROPERTIES:
:Suggested_File: core/vm_accel.hpp core/vm_accel.cpp
:END:

Move acceleration parameters and accelerated function implementations into
their own file. This is Inform-specific support and should not visually
dominate the generic execution loop.

Implemented in ~core/vm_accel.hpp~ and ~core/vm_accel.cpp~.

* DONE Create shared opcode metadata
:PROPERTIES:
:Area: decode execute trace
:Value: high
:Suggested_File: core/opcode_meta.hpp core/opcode_meta.cpp
:END:

Opcode facts are currently spread across decode, execute, and tracing logic.
Create one shared source of truth for:

- opcode names
- operand counts
- store operand positions
- special load widths
- trace annotations where they are mechanical

Keep this modest at first. The goal is not to rewrite the interpreter into a
table-driven VM, just to stop duplicated opcode knowledge from drifting.

** DONE Centralize operand counts and operand roles

Implemented in ~core/opcode_meta.hpp~ and ~core/opcode_meta.cpp~. Decode now
uses the shared operand-count table, and execution uses the shared store/load
operand metadata.

** DONE Move opcode names into shared metadata

~opcode_name~ now lives behind the same metadata module, so decode, trace, and
diagnostics do not depend on decode-specific implementation details for naming.

* TODO Split common Glk runtime into smaller modules
:PROPERTIES:
:Area: glk
:Value: high
:END:

~core/glk_runtime.hpp~ is much more coherent than the old transcript runtime,
but it is already dense. Keep ~GlkSession<Host>~ as the central coordinator and
separate the common runtime concepts underneath it.

** DONE Separate Glk registry and handle records
:PROPERTIES:
:Suggested_File: core/glk_registry.hpp
:END:

Extract handle allocation, lookup, iteration, and record storage for windows,
streams, and file refs.

*** DONE Extract typed window and stream records

Implemented in ~core/glk_registry.hpp~. The runtime still owns lookup,
allocation, and iteration, but the host-parameterized record types are no
longer embedded in ~GlkSession~.

*** DONE Extract allocation, lookup, and iteration helpers

Implemented in ~GlkRegistry<Host>~. ~GlkSession~ now delegates window/stream
lookup, allocation, and iteration to the registry.

** TODO Separate Glk stream behavior
:PROPERTIES:
:Suggested_File: core/glk_streams.hpp
:END:

Extract current stream state, window streams, memory streams, unicode memory
streams, stream closing, and stream result accounting.

Memory streams are common Glk behavior, not host-specific platform IO.

*** DONE Extract memory stream validation and writes

Implemented in ~core/glk_streams.hpp~. Byte and Unicode memory stream writes no
longer live inside ~GlkSession~.

*** DONE Extract stream record write routing

~glk_write_to_stream_record~ now owns host-stream writes, memory-stream writes,
and write-count accounting.

*** TODO Separate current stream and window-stream orchestration

~write~, ~write_to~, ~close_stream~, ~set_current_stream~, and ~set_window~
still live in ~GlkSession~ because they coordinate handles and current stream
state.

** TODO Separate Glk event/select state
:PROPERTIES:
:Suggested_File: core/glk_events.hpp
:END:

Extract pending line/char event requests, event result writeback, cancellation,
and select-blocking state.

The host should receive semantic wait requests; bridge bookkeeping should stay
in common runtime code.

*** DONE Extract event writeback and pending line buffer data

Implemented in ~core/glk_events.hpp~ and ~core/glk_events.cpp~. This covers
Glk reference writes, event structure field writes, pending line request data,
and line-input text writeback into VM memory.

*** TODO Separate select orchestration from GlkSession

~select~, ~write_event~, ~write_line_event~, and echo handling still live in
~GlkSession~ because they coordinate host calls with stream writes.

** DONE Separate Glk text materialization
:PROPERTIES:
:Suggested_File: core/glk_text.hpp
:END:

Extract VM-addressed text references and helpers that materialize or iterate
over text for host writes.

Hosts should not need to understand raw VM memory encodings just to display
text.

Implemented in ~core/glk_text.hpp~ and ~core/glk_text.cpp~. This includes text
materialization, text length, and Unicode case-buffer helpers.

** TODO Separate raw Glk selector dispatch
:PROPERTIES:
:Suggested_File: core/glk_dispatch.hpp
:END:

Keep selector decoding and argument/result adaptation separate from semantic
runtime operations. This preserves a clean boundary between the VM-facing C-ish
Glk ABI and the host-facing interface.

*** DONE Extract raw argument adapter

Implemented ~GlkArgs~ in ~core/glk_dispatch.hpp~. The selector switch now uses
the adapter for raw argument reads instead of indexing the VM argument span
directly.

*** TODO Move remaining selector dispatch structure out of GlkSession

The main selector switch still lives in ~GlkSession::call~.

* DONE Move trace duplication into shared helpers
:PROPERTIES:
:Area: tools
:Value: medium
:END:

~tools/glupsk-trace.cpp~ currently duplicates several pieces of interpreter
knowledge. After the VM execution split, make the trace tool mostly formatting
and presentation code.

** DONE Reuse operand inspection helpers from VM operand module

Trace operand rendering should use the same addressing rules as execution,
without mutating machine state.

Implemented with ~glupsk::peek_operand~ in ~core/vm_operands.hpp~.

** DONE Share Glk selector and gestalt names
:PROPERTIES:
:Suggested_File: core/glk_names.hpp core/glk_names.cpp
:END:

Move Glk selector and gestalt name tables out of the trace tool so errors,
logs, and future tools can use the same names.

Implemented in ~core/glk_names.hpp~ and ~core/glk_names.cpp~.

** DONE Share small integer/sign helpers

Avoid tiny duplicated helpers such as sign extension where a common helper
would make execution and tracing visibly agree.

Trace now reuses ~glupsk::sign_extend~ from ~core/vm_operands.hpp~.

* DONE Make stream opcode output blocking semantics explicit
:PROPERTIES:
:Area: effects glk
:Value: medium
:END:

~@glk~ calls can now return a blocked result and resume cleanly. Non-~@glk~
stream opcodes such as ~streamchar~, ~streamstr~, and ~streamunichar~ still look
synchronous.

Decide whether all output should participate in the same effect-handler style
blocking/resume model. If yes, design a resumable representation for blocked
stream output opcodes.

Decision: stream opcode output remains synchronous for now and fails loudly if
the host blocks outside an ~@glk~ call. This is documented in
~core/vm_strings.cpp~ at the stream opcode output boundary. A future resumable
opcode state can replace that behavior when output blocking becomes necessary.

* WAIT Consider splitting the test harness
:PROPERTIES:
:Area: tests
:Value: low
:END:

~tests/test.hpp~ is large, but it is self-contained and not currently the main
architecture pressure. Revisit only if it starts slowing test work.

Possible split:

- registration and suite discovery
- filtering and command-line handling
- reporting
- assertions and exception helpers

* Notes

- ~core/machine.*~, ~core/story.*~, and ~core/decode.*~ are currently fairly
  cohesive compared with the execution and Glk runtime files.
- Prefer extracting real conceptual modules over making many tiny files.
- Keep the VM state easy to serialize, but do not force host/runtime helper
  structures to be pointerless when they are not VM state.
- Keep platform-specific IO in hosts; keep common Glulx and Glk semantics in
  reusable runtime code.
